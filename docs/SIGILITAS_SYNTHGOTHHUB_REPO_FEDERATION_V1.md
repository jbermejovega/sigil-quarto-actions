# SIGILITAS SynthGothHub repository federation

The `sigil-quarto-actions` project tree is projected into a virtual repository session, an embodied STATIK memory checkpoint, a repository-matrix slot, and a typed federation rooted at `jbermejovega/sigilbook`.

Each presentation has a distinct bearer. The shared omega witness binds their source repository, head commit, and whole-tree digest without asserting literal identity. Active STATIK memory may be reconciled; its checkpoint ledger is monotonic.

`SynthGothHubCloudType` is an abstract federation projection. It does not claim a physical cloud, hidden synchronization, GitHub mutation, or runtime authority. Federation capabilities compose by intersection.
